# CRM Celery Report

## Prereqs
- Redis installed and running at `redis://localhost:6379/0`
  - macOS (Homebrew): `brew install redis && brew services start redis`
- Python deps:
  ```bash
  pip install -r requirements.txt
